#include <MP3.h>

MP3 redLight(//put pin number here);
MP3 greenLight(//put pin number here);
MP3 beginning(//put pin number here);
MP3 gunshots(//put pin number here);

void setup() {
  
}

void loop() {
  greenLight.myFunction(//put number that you want the noise rate to be in ms);
  redLight.myFunction(//put number that you want the noise rate to be in ms);
  beginning.myFunction(//put number that you want the noise rate to be in ms);
  gunshots.myFunction(//put number that you want the noise rate to be in ms);
}